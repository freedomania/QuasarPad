<Window x:Class="QuasarPad.Views.RegexTesterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Regex Tester - QuasarPad" Height="560" Width="800"
        MinHeight="400" MinWidth="600" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}">
  <DockPanel Margin="10">
    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
      <TextBlock Text="Pattern" Foreground="{DynamicResource EditorForeground}" Margin="0,0,0,4"/>
      <TextBox x:Name="PatternBox" Height="28" FontFamily="Consolas"
               Background="{DynamicResource EditorBackground}" Foreground="{DynamicResource EditorForeground}"/>
      <CheckBox x:Name="ChkIgnoreCase" Content="Ignore case" Margin="0,6,0,0"
                Foreground="{DynamicResource EditorForeground}"/>
      <Button Content="Test" Margin="0,8,0,0" Padding="12,4" HorizontalAlignment="Left" Click="Test_Click"/>
    </StackPanel>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="6"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <DockPanel Grid.Row="0">
        <TextBlock DockPanel.Dock="Top" Text="Input" Foreground="{DynamicResource EditorForeground}" Margin="0,0,0,4"/>
        <TextBox x:Name="InputBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" Background="{DynamicResource EditorBackground}"
                 Foreground="{DynamicResource EditorForeground}"/>
      </DockPanel>
      <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch"/>
      <DockPanel Grid.Row="2">
        <TextBlock DockPanel.Dock="Top" Text="Result" Foreground="{DynamicResource EditorForeground}" Margin="0,0,0,4"/>
        <TextBox x:Name="ResultBox" AcceptsReturn="True" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontFamily="Consolas"
                 Background="{DynamicResource EditorBackground}" Foreground="{DynamicResource EditorForeground}"/>
      </DockPanel>
    </Grid>
  </DockPanel>
</Window>
