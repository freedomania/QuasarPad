<Window x:Class="QuasarPad.Views.TextToolsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Tools - QuasarPad"
        Height="520" Width="720"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}">

    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" Text="Text Tools (Offline)" FontSize="16" FontWeight="SemiBold"
                   Foreground="{DynamicResource EditorForeground}" Margin="0,0,0,10"/>

        <WrapPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <Button Content="Base64 Encode" Click="Base64Encode_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="Base64 Decode" Click="Base64Decode_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="URL Encode" Click="UrlEncode_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="URL Decode" Click="UrlDecode_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="UPPERCASE" Click="Upper_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="lowercase" Click="Lower_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="Title Case" Click="TitleCase_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="Reverse" Click="Reverse_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="MD5" Click="Md5_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="SHA256" Click="Sha256_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="Remove Empty Lines" Click="RemoveEmpty_Click" Margin="0,0,6,6" Padding="10,5"/>
            <Button Content="Copy Result" Click="Copy_Click" Margin="0,0,6,6" Padding="10,5"/>
        </WrapPanel>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0">
                <TextBlock DockPanel.Dock="Top" Text="Input" Foreground="{DynamicResource EditorForeground}" Margin="0,0,0,4"/>
                <TextBox x:Name="InputBox" AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="13"
                         Background="{DynamicResource EditorBackground}"
                         Foreground="{DynamicResource EditorForeground}"
                         CaretBrush="{DynamicResource EditorForeground}"/>
            </DockPanel>
            <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}"/>
            <DockPanel Grid.Row="2">
                <TextBlock DockPanel.Dock="Top" Text="Result" Foreground="{DynamicResource EditorForeground}" Margin="0,0,0,4"/>
                <TextBox x:Name="OutputBox" AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="13"
                         Background="{DynamicResource EditorBackground}"
                         Foreground="{DynamicResource EditorForeground}"/>
            </DockPanel>
        </Grid>
    </DockPanel>
</Window>
